apiVersion: v1
kind: Service
metadata:
  labels:
    apiserver: "true"
  name: api-server-lb
  namespace: openshift-kube-apiserver
spec:
  selector:
    apiserver: "true"
  ports:
  - name: https
    port: 6443
    targetPort: 6443
    protocol: TCP
  type: LoadBalancer
